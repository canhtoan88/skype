const dotenv = require('dotenv');
const sendMessage = require('./send-message');
dotenv.config();

sendMessage('tôi đang dùng trick nên nó ngu lắm, giờ tôi phải tìm cách làm sao để nó thông minh nhất có thể');
